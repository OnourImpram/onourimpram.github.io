const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),ts=require('typescript'),vm=require('node:vm');
function lib(){const m={exports:{}};vm.runInNewContext(ts.transpileModule(fs.readFileSync('src/lib/desk-v8.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020}}).outputText,{module:m,exports:m.exports,URLSearchParams});return m.exports;}
test('V9 defaults to two bookcases in an atelier',()=>{const x=lib();assert.equal(x.defaultStudio.shelves,'both');assert.equal(x.defaultStudio.room,'atelier');assert.equal(x.defaultStudio.shelfLight,65)});
test('room choices survive shared URL while private fields stay out',()=>{const x=lib(),a=x.normalizeStudio({...x.defaultStudio,shelves:'right',room:'product',shelfLight:33,note:'PRIVATE'});const s=x.studioQuery(a);assert.equal(JSON.stringify(x.studioFromQuery(s)),JSON.stringify(a));assert.ok(!s.includes('PRIVATE'))});
test('old V8 URL gets safe V9 scene defaults',()=>{const x=lib(),a=x.studioFromQuery('en=203&yukseklik=113&malzeme=mese');assert.equal(a.width,203);assert.equal(a.height,113);assert.equal(a.shelves,'both');assert.equal(a.room,'atelier')});
test('invalid lighting and room input fall back safely',()=>{const x=lib(),a=x.normalizeStudio({shelfLight:Infinity,shelves:'script',room:'script'});assert.equal(a.shelfLight,65);assert.equal(a.shelves,'both');assert.equal(a.room,'atelier')});
