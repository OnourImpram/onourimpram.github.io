# V11 execution ledger

Baseline remote cb8cf2af5f7044327dc4744014bc26baa53853a4.

A. Implemented. Active source, system prefill, authored note and public studio configuration are separate. Source cycles, external-source clearing, studio/form round trips and numeric uncertainty are tested. Decimal or out-of-range form measurements remain explicit instead of being silently rounded into the 3D visual range.
B. Implemented. The complete visible message is the same text used for TXT, ZIP and WhatsApp. Long messages have a separately visible short WhatsApp introduction and complete file alternative. Search includes the studio and controlled Turkish aliases, exact inspiration targets and contextual contact. Back navigation restores list position. Metadata is page-specific.
C. Implemented. Mobile scene and active height control fit simultaneously at 320, 360 and 390 pixels. Fullscreen contains the controls and restores focus. The optional footprint is derived from rendered table geometry and explicitly excludes room/decor and any engineering guarantee. Real mobile GPU and virtual-keyboard tests are manual gates.
D. Implemented as supported. Homepage uses a single category discovery path, early real work, concise craft/process and a studio invitation. One project form uses optional details. Three real project explanations describe only observed layouts and questions, not invented original briefs or material specifications.
E. External gates remain open. No commercial-host migration, no new domain, no invented approvals, no real message sent, no order or payment backend. Existing GitHub Pages noindex preview is the requested publication target.
F. Automated source tests and offline browser checks completed. HTTP staging and live checks pending at this ledger snapshot.
Rename. Active source, generated HTML, metadata, accessible text and outgoing message templates use Yunus Usta. The business phone is unchanged. Historical audit documents remain historical evidence.

Ruling. User approved the complete audit plan and publication. An isolated source copy and separate GitHub staging branch protect the existing release. Only the elif-tasarim subtree may be published. Human device tests, business terms, photo permissions and real WhatsApp receipt cannot be fabricated.

Verification notes.
The V10 baseline passed 90 tests. The first V11 core tests failed as expected against that baseline. The current unit and build group passes 114 tests. A further new HTTP test reproduced the outdated preview server manifest lookup as a 404 and passed after adding V11. An offline image-alias regression was likewise reproduced before correction.
The primary browser suite has 25 passing named groups. The structural matrix has 301 route/viewport combinations with no failed structure checks. Three controlled doubled-font probes pass. These are not 301 independent user journeys or WCAG certification.
Early browser-script fixture errors used wrong accessible labels and an insufficiently long Unicode message, and were corrected without changing the product to fit a false assertion. A first idle probe observed one queued render after keyboard movement. The follow-up waits for the scene to settle before testing an idle interval. These probe corrections do not establish device speed or physical frame rates.
