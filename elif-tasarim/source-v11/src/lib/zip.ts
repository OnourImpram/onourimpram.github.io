/** Minimal ZIP STORE writer for private, local exports. No network or third-party service. */
export async function localZip(entries:{name:string;blob:Blob}[]):Promise<Blob>{
 let offset=0;const bodies:Uint8Array[]=[],central:Uint8Array[]=[];const encoder=new TextEncoder();
 const crc=(data:Uint8Array)=>{let c=0xffffffff;for(const n of data){c^=n;for(let i=0;i<8;i++)c=(c>>>1)^((c&1)?0xedb88320:0)}return (c^0xffffffff)>>>0};
 for(const e of entries){const name=encoder.encode(e.name.replace(/[^A-Za-z0-9_.-]/g,'_')),data=new Uint8Array(await e.blob.arrayBuffer()),checksum=crc(data);const h=new Uint8Array(30+name.length),v=new DataView(h.buffer);v.setUint32(0,0x04034b50,true);v.setUint16(4,20,true);v.setUint16(6,0x800,true);v.setUint16(12,33,true);v.setUint32(14,checksum,true);v.setUint32(18,data.length,true);v.setUint32(22,data.length,true);v.setUint16(26,name.length,true);h.set(name,30);const c=new Uint8Array(46+name.length),cv=new DataView(c.buffer);cv.setUint32(0,0x02014b50,true);cv.setUint16(4,20,true);cv.setUint16(6,20,true);cv.setUint16(8,0x800,true);cv.setUint16(14,33,true);cv.setUint32(16,checksum,true);cv.setUint32(20,data.length,true);cv.setUint32(24,data.length,true);cv.setUint16(28,name.length,true);cv.setUint32(42,offset,true);c.set(name,46);bodies.push(h,data);central.push(c);offset+=h.length+data.length;}
 const count=entries.length,csize=central.reduce((n,x)=>n+x.length,0),end=new Uint8Array(22),ev=new DataView(end.buffer);ev.setUint32(0,0x06054b50,true);ev.setUint16(8,count,true);ev.setUint16(10,count,true);ev.setUint32(12,csize,true);ev.setUint32(16,offset,true);return new Blob([...bodies,...central,end] as BlobPart[],{type:'application/zip'});
}
export function saveBlob(name:string,blob:Blob){const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),20000)}
